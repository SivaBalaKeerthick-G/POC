# ─────────────────────────────────────────────────────────────
# CogniDoc — Global requirements for all 5 microservices
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12       # multipart file uploads

# Data validation
pydantic==2.13.5
pydantic-settings==2.15.0

# Database — PostgreSQL (async)
sqlalchemy[asyncio]==2.0.35
asyncpg==0.31.0

# Vector store — ChromaDB & LangChain integration
chromadb==1.5.9
langchain-chroma==1.1.0

# LangChain core & integrations
langchain==1.4.0
langchain-core==1.6.2
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-google-genai==4.4.0
langchain-groq==1.1.3

# AI SDKs
google-genai==2.22.0
groq==0.37.1

# Document extraction
PyMuPDF==1.26.1                # PDF
python-docx==1.1.2             # DOCX
openpyxl==3.1.5                # XLSX
xlrd==2.0.1                    # XLS (legacy Excel)

# RAG utilities
rank-bm25==0.2.2               # BM25 sparse retrieval
tiktoken==0.14.0               # token counting for chunking

# HTTP client (gateway proxy + inter-service calls)
httpx==0.28.1

# Auth0 JWT validation
python-jose[cryptography]==3.3.0
